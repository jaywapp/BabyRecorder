# IDE specific files
.vscode/
.idea/
*.iml

# Log files
*.log
log/
tmp/

# Temporary files
*.tmp
*.bak
*.swp
*~

# Apps Script related (examples, adjust as needed)
# Apps Script는 기본적으로 클라우드 기반이므로 로컬에 직접적인 빌드 파일이 많지 않을 수 있습니다.
# 하지만 개발 환경이나 로컬 테스트를 위한 파일이 생긴다면 추가합니다.
node_modules/  # 만약 Node.js를 사용한다면
package-lock.json # Node.js 사용 시
yarn.lock # Yarn 사용 시

# Sensitive files (placeholder - actual file names may vary)
# 실제 민감한 정보가 담긴 파일명 (예: API 키, 인증서 등)은 여기에 추가합니다.
# config.js # 설정 파일이 별도로 있다면
# .env # 환경 변수 파일이 있다면